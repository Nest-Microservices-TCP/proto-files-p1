syntax = "proto3";

package rooms.rates;

import "rates-response.proto";
import "rates-request.proto";

service RatesService {
  rpc findOne (rooms.rates.FindOneRateRequest) returns (rooms.rates.Rate);
}